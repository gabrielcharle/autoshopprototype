<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; background-color: #f4ffec; }
        .container { max-width: 900px; margin: auto; padding: 30px; background-color: white; border-radius: 10px; box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1); }
        h1 { color: #2e8b57; border-bottom: 2px solid #2e8b57; padding-bottom: 10px; }
        .action-links a { display: inline-block; background-color: #007bff; color: white; padding: 10px 20px; text-decoration: none; border-radius: 5px; margin-right: 15px; margin-bottom: 20px; }
        .action-links a:hover { background-color: #0056b3; }
        .dashboard-list { list-style-type: none; padding: 0; }
        .dashboard-list li { margin-bottom: 10px; }
        .dashboard-list a { text-decoration: none; color: #333; font-weight: bold; padding: 8px; display: block; background-color: #e9ecef; border-left: 5px solid #2e8b57; border-radius: 4px; }
        .dashboard-list a:hover { background-color: #d1d8df; }
        /* Add CSS for Feedback Message */
        .feedback-box { margin-bottom: 20px; padding: 15px; border-radius: 5px; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Inventory Management System Main Menu</h1>
        
        <% if (message && status) { %>
            <div class="feedback-box" 
                 style="background-color: <%= status === 'success' ? '#d4edda' : '#f8d7da' %>;
                        color: <%= status === 'success' ? '#155724' : '#721c24' %>;
                        border: 1px solid <%= status === 'success' ? '#c3e6cb' : '#f5c6cb' %>;">
                
                <%= message %>
            </div>
        <% } %>

        <h2>📝 Transaction Hub</h2>
        <div class="action-links">
            <a href="/receive">Receive Stock</a>
            <a href="/issue">Issue Stock</a>
        </div>
        
        <h2>📊 Reporting & Dashboards</h2>
        <ul class="dashboard-list">
            <% dashboards.forEach((name, index) => { %>
                <li>
                    <a href="/dashboard/<%= index + 1 %>">Dashboard <%= index + 1 %>: <%= name %></a>
                </li>
            <% }); %>
        </ul>
        
    </div>
</body>
</html>